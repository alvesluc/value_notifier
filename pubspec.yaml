name: value_notifier
description: A simple Flutter project aimed to use and test the ValueNotifier, using the state pattern.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  uno: ^1.0.1+3
  provider: ^6.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  mocktail: ^0.2.0
  value_listenable_test: ^0.0.1

flutter:
  uses-material-design: true
